import { SectionContainer } from "src/components/common";
import { sectionIds } from "src/constants/sectionIds";

const Section4 = () => {
  return (
    <section id={sectionIds.section4}>
      <SectionContainer>Section4</SectionContainer>
    </section>
  );
};

export default Section4;
