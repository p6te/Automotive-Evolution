import { SectionContainer } from "src/components/common";
import { sectionIds } from "src/constants/sectionIds";

const Section5 = () => {
  return (
    <section id={sectionIds.section5}>
      <SectionContainer>Section5</SectionContainer>
    </section>
  );
};

export default Section5;
